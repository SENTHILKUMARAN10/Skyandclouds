import { integer, sqliteTable, text, index } from "drizzle-orm/sqlite-core";
export const rooms = sqliteTable("rooms", {id:text("id").primaryKey(),name:text("name").notNull(),mediaUrl:text("media_url").notNull().default(""),playbackTime:integer("playback_time").notNull().default(0),playbackState:text("playback_state").notNull().default("paused"),updatedAt:integer("updated_at").notNull(),createdAt:integer("created_at").notNull()});
export const participants = sqliteTable("participants", {id:text("id").primaryKey(),roomId:text("room_id").notNull(),name:text("name").notNull(),color:text("color").notNull().default("#7c6cff"),lastSeen:integer("last_seen").notNull()},t=>[index("idx_participants_room_seen").on(t.roomId,t.lastSeen)]);
export const messages = sqliteTable("messages", {id:integer("id").primaryKey({autoIncrement:true}),roomId:text("room_id").notNull(),participantId:text("participant_id").notNull(),name:text("name").notNull(),body:text("body").notNull(),kind:text("kind").notNull().default("chat"),createdAt:integer("created_at").notNull()},t=>[index("idx_messages_room_id").on(t.roomId,t.id)]);
